{
  "required_fields": [
    "timestamp",
    "ws_id",
    "trace_id",
    "event.type",
    "event.source",
    "principal.id",
    "principal.role",
    "target.sample_id",
    "target.dataset_id",
    "target.purpose",
    "decision.outcome",
    "decision.reason_code",
    "decision.baseline_hash",
    "enforcement.result",
    "metrics.time_to_decide_ms",
    "metrics.read_granted"
  ],
  "notes": {
    "privacy": "Evidence must not include personal data. Use synthetic IDs and redacted metadata.",
    "read_granted": "Must be false under deny/quarantine for this pack's vectors."
  }
}
