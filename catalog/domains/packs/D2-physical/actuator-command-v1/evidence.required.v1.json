{
  "required_fields": [
    "timestamp",
    "ws_id",
    "trace_id",
    "event.type",
    "event.source",
    "principal.id",
    "principal.role",
    "target.actuator_id",
    "target.command_id",
    "target.command_params",
    "decision.outcome",
    "decision.reason_code",
    "decision.baseline_hash",
    "enforcement.result",
    "metrics.time_to_decide_ms",
    "metrics.command_executed"
  ],
  "notes": {
    "minimal_disclosure": "Do not emit raw secrets or environment-specific details. Emit stable IDs and redacted params if needed.",
    "command_executed": "Must be false under deny/quarantine for this pack's vectors (simulation-only)."
  }
}
