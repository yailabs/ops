{
  "vector_id": "safe-actuator-cmd-001",
  "expected": [
    {
      "baseline_id": "baseline-allow",
      "decision": { "outcome": "deny", "reason_code": "ACTUATOR_NOT_CONTRACTED" },
      "enforcement": { "result": "blocked" },
      "metrics": { "command_executed": false }
    },
    {
      "baseline_id": "baseline-deny",
      "decision": { "outcome": "deny", "reason_code": "ACTUATOR_NOT_CONTRACTED" },
      "enforcement": { "result": "blocked" },
      "metrics": { "command_executed": false }
    },
    {
      "baseline_id": "baseline-quarantine",
      "decision": { "outcome": "deny", "reason_code": "ACTUATOR_NOT_CONTRACTED" },
      "enforcement": { "result": "blocked" },
      "metrics": { "command_executed": false }
    }
  ],
  "notes": {
    "safety_window": "This vector primarily tests contracted-actuator enforcement. Add a second vector later to test SAFETY_WINDOW_VIOLATION explicitly with a contracted actuator but outside allowed window.",
    "arming": "If arming state is modeled in v0.1 execution, expected outcomes may include ARMING_REQUIRED instead of ACTUATOR_NOT_CONTRACTED when actuator is contracted."
  }
}
