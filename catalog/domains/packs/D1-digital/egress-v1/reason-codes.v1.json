{
  "EGRESS_DEST_NOT_CONTRACTED": {
    "severity": "high",
    "default_outcome": "deny",
    "description": "Destination is not present in workspace egress contract/allowlist."
  },
  "EGRESS_PORT_NOT_ALLOWED": {
    "severity": "high",
    "default_outcome": "deny",
    "description": "Destination port is not allowed by the workspace egress contract."
  },
  "EGRESS_DNS_NOT_CONTRACTED": {
    "severity": "medium",
    "default_outcome": "deny",
    "description": "Resolved hostname or DNS target is not allowed by contract (if hostname-based policies are enabled)."
  },
  "EGRESS_THRESHOLD_EXCEEDED": {
    "severity": "medium",
    "default_outcome": "rate_limit",
    "description": "Egress attempts exceed configured thresholds (burst/rate)."
  }
}
