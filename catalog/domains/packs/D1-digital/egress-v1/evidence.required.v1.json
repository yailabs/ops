{
  "required_fields": [
    "timestamp",
    "ws_id",
    "trace_id",
    "event.type",
    "event.source",
    "principal.id",
    "principal.role",
    "target.dst.ip",
    "target.dst.port",
    "decision.outcome",
    "decision.reason_code",
    "decision.baseline_id",
    "decision.baseline_hash",
    "enforcement.result",
    "metrics.time_to_contain_ms",
    "metrics.bytes_exfiltrated",
    "metrics.connect_established",
    "metrics.target_reachable"
  ],
  "notes": {
    "minimal_disclosure": "Evidence must not leak secrets. Only include what is required to prove decision + enforcement + context.",
    "connect_established": "If bytes_exfiltrated is not measurable yet, connect_established=false is still required for deny/quarantine.",
    "target_reachable": "Target reachability must be recorded independently from policy outcome to avoid false positives from network failures."
  }
}
