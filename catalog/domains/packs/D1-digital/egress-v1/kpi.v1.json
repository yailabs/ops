{
  "kpis": [
    {
      "id": "time_to_contain_ms",
      "description": "Latency from trigger observation to enforcement result.",
      "unit": "ms",
      "target": { "p50_max": 10, "p95_max": 50 }
    },
    {
      "id": "bytes_exfiltrated",
      "description": "Bytes successfully transmitted outside the workspace boundary for forbidden effects.",
      "unit": "bytes",
      "target": { "max": 0 }
    },
    {
      "id": "connect_established",
      "description": "Whether an outbound connection was successfully established.",
      "unit": "bool",
      "target": { "must_be_false_under_deny_or_quarantine": true }
    },
    {
      "id": "evidence_completeness",
      "description": "Percentage of required evidence fields present.",
      "unit": "percent",
      "target": { "min": 100 }
    },
    {
      "id": "repeatability_runs",
      "description": "Number of consecutive consistent PASS runs required for qualification.",
      "unit": "count",
      "target": { "min": 3 }
    }
  ],
  "notes": {
    "thresholds": "Targets are v0.1 goals; they may be relaxed during early implementation but must be recorded explicitly when deviating."
  }
}
