{
  "required_fields": [
    "timestamp",
    "ws_id",
    "trace_id",
    "event.type",
    "event.source",
    "principal.id",
    "principal.role",
    "target.station_id",
    "target.region_id",
    "target.measurement_type",
    "target.units",
    "target.value",
    "provenance.ref",
    "calibration.ref",
    "decision.outcome",
    "decision.reason_code",
    "decision.baseline_hash",
    "enforcement.result",
    "metrics.time_to_decide_ms",
    "metrics.ingest_committed"
  ],
  "notes": {
    "privacy": "Use region_id (coarse) instead of exact coordinates. Avoid storing raw location data unless required.",
    "ingest_committed": "Must be false under deny/quarantine for this pack's vectors."
  }
}
